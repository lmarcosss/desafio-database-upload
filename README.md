##### API do GoFinances. Foi feito pelo desafio GoStack da Rocketseat.

Framework utilizado:
* Express + Typescript

Para instalar as dependências: ` yarn install `

Para rodar o projeto: ` yarn start `
